# nice-polyglot Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-25

## Active Technologies

- PowerShell 7 (pwsh) for config resolution scripts; YAML for config files; + SpecKit v0.5.1+; `common.ps1` for `.specify` root discovery (001-polyglot-config-extension)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for PowerShell 7 (pwsh) for config resolution scripts; YAML for config files;

## Code Style

PowerShell 7 (pwsh) for config resolution scripts; YAML for config files;: Follow standard conventions

## Recent Changes

- 001-polyglot-config-extension: Added PowerShell 7 (pwsh) for config resolution scripts; YAML for config files; + SpecKit v0.5.1+; `common.ps1` for `.specify` root discovery

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
